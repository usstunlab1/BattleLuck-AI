# BattleLuck Client Lab

Vite browser test harness for BattleLuck AI enrichment routes.

```bash
cd client
npm install
npm run dev
```

Default URL: `http://localhost:5174`

- **direct** mode: calls remote URL from the browser (requires CORS-safe backend)
- **proxy** mode: uses Vite dev proxy at `/battleluck-api`
